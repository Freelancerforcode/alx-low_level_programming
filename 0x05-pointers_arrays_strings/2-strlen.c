#include <stdio.h>
/**
 * main - int _strlen(char *s);
 * function:  that returns the length of a string.
 * Return: 0
 */
int main(void)
{
	int _strlen(char *s);
	int len;

	len = strlen(*s);
	_putchar("strlen (char *s): %d\n", *s);

	return (0);
}
